name: repositories
description: Dart package which manages all the repositories for the project.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  equatable: ^2.0.0
  rxdart: ^0.27.1
  uuid: ^3.0.4
  services:
    path: ../services
  encrypt: ^5.0.1
